name: Build Android APK

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        cache-dependency-path: 'ClipboardPro_XClipper/gradle/wrapper/gradle-wrapper.properties'

    - name: Grant execution permission for gradlew
      run: chmod +x "ClipboardPro_XClipper/gradlew"

    - name: Build Debug APK
      run: ./gradlew assembleDebug
      working-directory: "ClipboardPro_XClipper"

    - name: Rename APK to ClipboardPro.apk
      run: mv "ClipboardPro_XClipper/app/build/outputs/apk/debug/app-debug.apk" "ClipboardPro_XClipper/app/build/outputs/apk/debug/ClipboardPro.apk"

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: ClipboardPro
        path: ClipboardPro_XClipper/app/build/outputs/apk/debug/ClipboardPro.apk
