<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main app theme — AppCompat parent needed for Compose/ComponentActivity -->
    <style name="Theme.ClipboardPro" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">#0D0D0F</item>
        <item name="android:statusBarColor">#0D0D0F</item>
        <item name="android:navigationBarColor">#1A1A1F</item>
    </style>

    <!-- Invisible theme for ClipboardCaptureActivity.
         Uses Translucent (not NoDisplay) so finish() can be called async
         without crashing. The window is zero-size and fully transparent. -->
    <style name="Theme.Transparent.Capture" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
